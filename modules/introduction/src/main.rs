fn main() {

    // PT 1

    // * Packages are cargo feature, that lets you build, test and share crates

    // * crates are trees of modules (We will see examples later)
    // ** Library crate 
    //     if the src/lib.rs exists
    //      
    // ** Binary crate
    //     if the src/main.rs exists

    // * modules are something that let you to organize your code
}
